this is learning of git

1 cd
2 clone <link of project>
3. status 
if changes add
4. add <filename>
eg. add index.html
5. commit -m "some message"
6. git push origin main //only after this command changes reflect in git

